spring:
  datasource:
    username: sa
    password:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:testdb;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1

  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate.id.optimizer.pooled.preferred: false
  flyway:
    enabled: false

custom:
  jwt:
    secret: i14Yy9GnDi28ac/ZtWIjiavJqz8iI0TGwOLzzKPvAq8KdzS2ABNfgv5yBNQYFPDohgxgMR+s/W5aTK7svlfYoQ==
    qr-secret: 1lteHAVZiVnUOzxV7DNzh78/sUSCUOEYrwqOHOjoSXWqiiHHAEKOI1e/DXtM/A73KNDOScGR8N2v6YWU9bdSLg==
    access-token-duration: 3600000
    refresh-token-duration: 1209600000

  dev:
    domain: localhost
    back-url: "http://${custom.dev.domain}:8080"
    front-url: "http://${custom.dev.domain}:3000"
  prod:
    domain: waitfair.shop
    back-url: "https://api.${custom.prod.domain}"
    front-url: "https://www.${custom.prod.domain}"
  site:
    domain: "${custom.dev.domain}"
    back-url: "${custom.dev.back-url}"
    front-url: "${custom.dev.front-url}"

security:
  password:
    bcrypt-strength: 4

sms:
  test:
    enabled: true
    fixed-code: "123456"

# S3
cloud:
  aws:
    region: ap-northeast-2
    s3:
      bucket: test-waitfair-bucket
    presigned:
      put-expire-minutes: 5
      get-expire-minutes: 10

# reCAPTCHA
google:
  captcha:
    secret-key: test-secret-key

# TossPayments
toss:
  payments:
    secret: test_gsk_docs_OaPz8L5KdmQXkzRz3y47BMw6

# ⚠️ 이 프로젝트는 Doppler를 사용합니다!
#
# 환경변수 관리는 Doppler로 이관되었습니다.
# 이 파일은 참고용으로만 남겨둡니다.
#
# Doppler 설정 방법:
# 1. Doppler CLI 설치: https://docs.doppler.com/docs/install-cli
# 2. 로그인: doppler login
# 3. 프로젝트 설정: doppler setup --project waitfair --config dev
# 4. 실행: doppler run -- ./gradlew bootRun
#
# 팀원이신 경우 Doppler 계정 초대를 요청하세요.

# ===== 로컬 개발용 환경변수 예시 (Doppler dev 환경) =====
DB_USER=root
DB_PASSWORD=1234
JWT_SECRET=your_jwt_secret_key_here_at_least_256_bits
JWT_ACCESS_TOKEN_DURATION=3600
JWT_REFRESH_TOKEN_DURATION=1209600
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
API_BASE_URL=localhost

# ===== 운영 환경 예시 (Doppler prd 환경) =====
# SUPABASE_URL=jdbc:postgresql://...
# SUPABASE_USERNAME=postgres...
# SUPABASE_PASSWORD=...
# REDIS_HOST=redis
# API_BASE_URL=your-domain.com
# FRONTEND_URL=https://www.your-domain.com
# BACKEND_URL=https://api.your-domain.com